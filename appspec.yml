version: 0.0
Resources:
  - TargetService:
      Type: AWS::Lambda::Function
      Properties:
        Name: "demo-ml"  # Your exact Lambda name
        Alias: "dev"     # Must exist (create if missing)
