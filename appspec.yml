version: 0.0
Resources:
  - TargetService:
      Type: AWS::Lambda::Function
      Properties:
        Name: "demo-ml"       # Your exact Lambda function name
        Alias: "dev"          # Must exist (create if missing)
        CurrentVersion: "$LATEST"  # Required field for container deployments
        TargetVersion: "$LATEST"   # Required field for container deployments